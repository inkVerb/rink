# admin verb

## Create a verb web-UI PAM user on an existing VPS
- `rink admin verb -n [ namespace ] -l [ tld ] -r [ rink-user ] -u [ verb-username ] -t [ admin | supervisor ] -e [ email ]`
- SSH via `sshrun`, then `/opt/verb/serfs/newverbadmin`
- Does not run from `addvps`
