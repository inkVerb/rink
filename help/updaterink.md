# update rink

## Refresh this rink from the inkVerb/rink repo
- `rink update rink`
- Same as `/opt/rink/run/updaterink`
- First time on a box that still has the old `updaterun`: clone to `/tmp` and run the new script once (do not `git clone` into `/opt/rink/rink` — that path is the dispatcher)
